** 강조
     다시 빌드될 테이블
       없음
     삭제될 클러스터형 인덱스
       없음
     생성될 클러스터형 인덱스
       없음
     데이터 문제가 있는 것 같습니다.
       [dbo].[postData].[commandCount] 열이 삭제되므로 데이터 손실이 발생할 수 있습니다.

** 사용자 작업
     삭제
       [dbo].[postData]에 대한 명명되지 않은 제약 조건 (DEFAULT 제약 조건)
       [dbo].[postData]에 대한 명명되지 않은 제약 조건 (DEFAULT 제약 조건)
     변경
       [dbo].[postData] (테이블)
     만들기
       [dbo].[foodData] (테이블)
       DEFAULT 제약 조건: [dbo].[foodData]에 대한 명명되지 않은 제약 조건 (DEFAULT 제약 조건)
       DEFAULT 제약 조건: [dbo].[foodData]에 대한 명명되지 않은 제약 조건 (DEFAULT 제약 조건)

** 지원 동작
     만들기
       DEFAULT 제약 조건: [dbo].[postData]에 대한 명명되지 않은 제약 조건 (DEFAULT 제약 조건)

[dbo].[postData].[commandCount] 열이 삭제되므로 데이터 손실이 발생할 수 있습니다.

