** 강조
     다시 빌드될 테이블
       없음
     삭제될 클러스터형 인덱스
       없음
     생성될 클러스터형 인덱스
       없음
     데이터 문제가 있는 것 같습니다.
       [dbo].[postData] 테이블의 postKind 열 형식이  NVARCHAR (20) NOT NULL에서  TINYINT NOT NULL(으)로 변경됩니다. 데이터가 손실될 수 있습니다.

** 사용자 작업
     변경
       [dbo].[postData] (테이블)

** 지원 동작

[dbo].[postData] 테이블의 postKind 열 형식이  NVARCHAR (20) NOT NULL에서  TINYINT NOT NULL(으)로 변경됩니다. 데이터가 손실될 수 있습니다.

